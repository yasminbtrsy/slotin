import 'package:flutter/material.dart';

class ManageCourtsScreen extends StatelessWidget {
  const ManageCourtsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Manage Courts')),
      body: const Center(child: Text('Manage Courts - Coming soon')),
    );
  }
}
